若n能被8整除输出”Yes”,否则输出”No”;

#include<stdio.h>
#include<string.h>
int main()
{
    char s[105];
    while(~scanf("%s", s))
    {
        int m = 0;
        int i = 0;
        for(i = 0; i< strlen(s); i++)
        {
            m = (m*10+ s[i] - '0')%8;//因为每一个A都可以分解成n*B+C，其中C为A%B的值，m就是A的拆解
        }
        if(m == 0)
        {
            printf("Yes\n");
        }
        else
        {
            printf("No\n");
        }
    }
    return 0;
}
