首先，旋转90度后，x,y的变换规律，

(0,0)-->(0,3)  (0,1)-->(1,3) (0,2)-->(2,3) (0,3)-->(3,3) (0,4)-->(4,3)

...

(3,0)-->(0,0) (3,1)-->(1,0) (3,2)-->(2,0) (3,3)-->(3,0) (3,4)-->(4,0)

由以上规律，得

 b[i][j] = a[n-1-j][i];//旋转规律，n表示行，m表示列



//使用了两个数组 一个空数组用来接收旋转后的数组

#include<stdio.h>
int main()
{
    int n = 0;
    int m = 0;
    scanf("%d%d", &n, &m);
    int a[100][100] = {0};
    int b[100][100] = {0};
    int i = 0;
    int j = 0;
    for(i = 0; i<n; i++)
    {
        for(j = 0; j<m; j++)
        {
            scanf("%d", &a[i][j]);
        }
    }
    for(i = 0; i<m; i++)
    {
        for(j = 0; j<n; j++)
        {
            b[i][j] = a[n-1-j][i];//旋转规律
        }
    }
    for(i = 0; i<m; i++)
    {
        for(j = 0; j<n; j++)
        {
            printf("%d ", b[i][j]);
        }
        printf("\n");
    }
    return 0;
}
