众所皆知，moxin有个牛逼的小老弟，但是moxin的小老弟最近异常的烦躁，image.png

Moxin为了给小老弟缓和心情，给小老弟买来了n个开关，给小老弟降压，但是小老弟的强迫症发作了，如果小老弟按了某个数都会把这个数的倍数全按一遍，比如按了2就会把2,4,6,8，、、、、、、全按一遍，一个开关被按一下就会改变状态，一开始所有的开关都是关闭的，小老弟从1开始按到了n（按的时候按照，先把所有的倍数按一遍，比如按完了2的所有倍数，接下来就按3的所有倍数），请问有几个开关是打开的？



Input
给出一个n（文件里多组数据）1<=n<=10^9

Output
给出开着的灯的数量

SampleInput
2
SampleOutput
1

总结
一个数被按的次数 等于他的 因子个数
一个数的 因子个数 为奇数 才亮
因子具有对称性
所以只有平方数的因子是奇数
那么只需计算小于 n 的平方数个数即可

#include<stdio.h>
#include<math.h>
int main()
{
 int n = 0;
 while(~scanf("%d",&n))
 {
     int i = 0;
     int light = 0;
     for(i = 1; i <= sqrt(n); i++)//i<sqrt(n) i*i 就是小于 n 的 平方数
     {
           light++;
     }
     printf("%d\n", light);
 }
 return 0;
}
