有n个无聊的人在比赛跳远，看谁跳的最远。每个人可以跳3次，这个人的成绩就是三次距离里面的最大值。
例如，一个人跳3次的距离分别时10, 30和20，那么这个人的成绩就是30。
给出每个人三次跳的距离，问最终每个人的排名是多少。
Input
输入文件的第一行有一个整数T (1≤T≤100)，表示测试数据的组数。
对于每组测试数据：第一行包括一个整数n (2≤n≤3), 表示人数.
接下来n行, 每行包含三个整数ai,bi 和 ci (1≤ai,bi,ci,≤300), 表示第i个人每次的跳的距离。
输入数据保证每个人的成绩互不相同。
Output
对于每组数据，输出一行包含n个整数，用一个空格隔开。
第i个数表示第i个人的最终名次。
#include<stdio.h>
int main()
{
    int T;
    scanf("%d", &T);
    while(T--)
    {
        int i,j,tmp,max,sum,n;
        int arr[3][305],b[5];
        scanf("%d", &n);
        if(2<=n && n<=3)
        {
            for(i= 0;i<n;i++)
            {
                for(j= 0;j<3;j++)
                {
                    scanf("%d", &arr[i][j]); //通过二维数组  存储 数据
                }
            }
        }
        for(i= 0;i<n;i++)
        {
            max = 0;
            for(j= 0;j< 3;j++)
            {
                if(max<arr[i][j])
                {
                    max = arr[i][j];//求出一个人的最好成绩
                }
                b[i] = max;//存储每个人的最好成绩
            }
        }
        for(i = 0; i<n; i++)
        {
            sum = 0;
            for(j = 0; j< n; j++)
            {
                if(b[i]<b[j])  //通过一个数据与所有数据的比较来确定最终排名
                {
                    sum++;
                }
            }
            if( i == n-1)
            {
                printf("%d\n", sum+1);
            }
            else
            {
                printf("%d ", sum+1);
            }
        }
    }
    return 0;
}
}
